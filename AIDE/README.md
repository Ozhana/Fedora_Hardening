# Aegis AIDE Shield
## Zero-Trust Asynchronous Intrusion Detection Architecture (NVMe & BTRFS Optimized)

| ENGLISH | TÜRKÇE |
| :--- | :--- |
| **Introduction**<br>This blueprint showcases the final **Aegis AIDE V18.1** decoupled intrusion monitoring architecture. Rather than triggering devastating, resource-heavy BTRFS block operations directly mid-DNF updates, it deposits an atomic state-change flag into the runtime environment. A hardened, low-priority Systemd execution engine then harvests this flag asynchronously during optimal scheduling windows, maximizing NVMe SSD block storage structural lifespan (TBW) and maintaining absolute host system integrity. | **Giriş**<br>Bu plan, nihai **Aegis AIDE V18.1** ayrıştırılmış (decoupled) saldırı tespit izleme mimarisini sunar. Her DNF güncellemesinin ortasında disk ömrünü tüketen ağır BTRFS blok operasyonlarını tetiklemek yerine, çalışma zamanı ortamına atomik bir durum değişim bayrağı bırakır. Katılaştırılmış, düşük öncelikli bir Systemd motoru bu bayrağı en uygun zaman dilimlerinde asenkron olarak işler; böylece hem NVMe SSD disk ömrünü (TBW) maksimum düzeyde korur hem de ana sistem bütünlüğünü muhafaza eder. |
| **Step 1: Bootstrap Core Packages**<br>Deploy the advanced detection environment and the official DNF core post-transaction trigger plugin.<br><br><code>sudo dnf install -y aide python3-dnf-plugin-post-transaction-actions</code> | **Adım 1: Çekirdek Paketleri Kurun**<br>Gelişmiş saldırı tespit ortamını ve resmi DNF işlem sonrası tetikleyici eklentisini sisteme kurun.<br><br><code>sudo dnf install -y aide python3-dnf-plugin-post-transaction-actions</code> |
| **Step 2: Generate the Master Baseline DB**<br>Establish your raw trusted system snapshot state. Forcefully build the initial metadata mapping and deploy it into the production vault path.<br><br><code>sudo aide --init</code><br><code>sudo mv -f /var/lib/aide/aide.db.new.gz /var/lib/aide/aide.db.gz</code> | **Adım 2: Ana Temel Çizgi Veritabanını Üretin**<br>Güvenilir sistem durum haritanızı oluşturun. İlk metaveri haritasını zorunlu olarak inşa edin ve üretim güvenli yoluna yerleştirin.<br><br><code>sudo aide --init</code><br><code>sudo mv -f /var/lib/aide/aide.db.new.gz /var/lib/aide/aide.db.gz</code> |
| **Step 3: Deploy the Asynchronous DNF Flag Engine**<br>Create the lightweight signaling hook script that flags software lifecycle modifications instantly without stalling DNF operations.<br><br><code>sudo nano /usr/local/bin/aegis-aide-flag.sh</code> | **Adım 3: Asenkron DNF Bayrak Motorunun Dağıtımı**<br>DNF işlemlerini milisaniyeler bile kilitlemeden, yazılım döngüsü değişikliklerini anında işaretleyen hafif kanca betiğini oluşturun.<br><br><code>sudo nano /usr/local/bin/aegis-aide-flag.sh</code> |
| **Flag Script Content:**<br>Paste the implementation exactly:<br><pre>#!/usr/bin/env bash<br>set -euo pipefail<br>touch /run/aegis_aide_pending.flag<br>exit 0</pre>Lock down parameters:<br><code>sudo chmod 700 /usr/local/bin/aegis-aide-flag.sh</code><br><br>Register the execution pathway directly with the DNF engine mapping subsystem:<br><code>sudo mkdir -p /etc/dnf/plugins/post-transaction-actions.d/</code><br><code>echo "*:in,out,up,rm:/usr/local/bin/aegis-aide-flag.sh" \| sudo tee /etc/dnf/plugins/post-transaction-actions.d/aide.action</code> | **Bayrak Betiği İçeriği:**<br>Uygulamayı aynen yapıştırın:<br><pre>#!/usr/bin/env bash<br>set -euo pipefail<br>touch /run/aegis_aide_pending.flag<br>exit 0</pre>Parametreleri kilitleyin:<br><code>sudo chmod 700 /usr/local/bin/aegis-aide-flag.sh</code><br><br>Çalıştırma yolunu doğrudan DNF motor eşleme alt sistemine kaydedin:<br><code>sudo mkdir -p /etc/dnf/plugins/post-transaction-actions.d/</code><br><code>echo "*:in,out,up,rm:/usr/local/bin/aegis-aide-flag.sh" \| sudo tee /etc/dnf/plugins/post-transaction-actions.d/aide.action</code> |
| **Step 4: The Ultimate Aegis AIDE Asynchronous Engine (V18.1)**<br>Deploy the bulletproof update and verification script. It features explicit path configurations to completely decouple from fragile config text parsing. It embeds file-level metadata synchronization (`sync -f`) directly before and after the atomic rename operation to eliminate 0-byte database corruption risks during unexpected power fluctuations.<br><br><code>sudo nano /usr/local/bin/aegis-aide-update.sh</code> | **Adım 4: Nihai Aegis AIDE Asenkron Motoru (V18.1)**<br>Kırılmaz güncelleme ve doğrulama betiğini konumlandırın. Kırılgan metin ayrıştırma işlemlerinden tamamen bağımsızlaşmak için statik yol tanımları içerir. Ani güç dalgalanmalarında yaşanabilecek 0-byte veritabanı yozlaşması risklerini yok etmek için atomik yeniden adlandırma işleminin hemen öncesine ve sonrasına dosya düzeyinde metaveri senkronizasyonu (`sync -f`) gömer.<br><br><code>sudo nano /usr/local/bin/aegis-aide-update.sh</code> |
| **Core Script Content:**<br>Paste the following production blueprint into the file:<br><pre>#!/usr/bin/env bash<br># V18_AEGIS_SURFACE: Asynchronous AIDE Updater & Auditor (V18.1)<br>set -euo pipefail<br>umask 0077<br>FLAG_FILE="/run/aegis_aide_pending.flag"<br>PROCESS_FLAG="/run/aegis_aide_processing.flag"<br>AUDIT_LOG="/var/log/aegis_aide_audit.log"<br>LOCK_FILE="/run/aegis_aide_lock"<br>AIDE_DB="/var/lib/aide/aide.db.gz"<br>AIDE_DB_NEW="/var/lib/aide/aide.db.new.gz"<br>AIDE_DIR="/var/lib/aide"<br>if [[ $EUID -ne 0 ]]; then<br>    systemd-cat -t aegis-aide -p err <<< "[FATAL] Root yetkisi gerekir."<br>    exit 1<br>fi<br>if ! mv "$FLAG_FILE" "$PROCESS_FLAG" 2>/dev/null; then<br>    exit 0<br>fi<br>exec 9>> "$LOCK_FILE"<br>if ! flock -n 9; then<br>    systemd-cat -t aegis-aide -p info <<< "[BİLGİ] Tarama zaten aktif. Durum korunuyor."<br>    if ! touch "$FLAG_FILE" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] Rollback bayrağı dikilemedi."<br>    fi<br>    if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] İşlem bayrağı temizlenemedi."<br>    fi<br>    exit 0<br>fi<br>cleanup() {<br>    local exit_code=$?<br>    if [[ $exit_code -ne 0 ]]; then<br>        systemd-cat -t aegis-aide -p emerg <<< "[KRİTİK] AIDE işlemi çöktü! Kod: $exit_code"<br>        if ! touch "$FLAG_FILE" 2>/dev/null; then<br>            systemd-cat -t aegis-aide -p warning <<< "[UYARI] Durum bayrağı geri yüklenemedi."<br>        fi<br>        if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>            systemd-cat -t aegis-aide -p warning <<< "[UYARI] Süreç bayrağı kaldırılamadı."<br>        fi<br>    fi<br>}<br>trap cleanup EXIT INT TERM<br>SUPPORTS_IMMUTABLE=false<br>if TEST_FILE=$(mktemp "${AIDE_DIR}/.aide_test.XXXXXX" 2>/dev/null); then<br>    if chattr +i "$TEST_FILE" 2>/dev/null; then<br>        SUPPORTS_IMMUTABLE=true<br>        chattr -i "$TEST_FILE" 2>/dev/null<br>    fi<br>    rm -f "$TEST_FILE"<br>else<br>    systemd-cat -t aegis-aide -p warning <<< "[UYARI] chattr testi başarısız."<br>fi<br>if [[ "$SUPPORTS_IMMUTABLE" == true && -f "$AIDE_DB" ]]; then<br>    chattr -i "$AIDE_DB" 2>/dev/null || true<br>fi<br>systemd-cat -t aegis-aide -p info <<< "[+] AIDE Güncelleme ve Denetim Operasyonu Başlatıldı..."<br>set +e<br>{<br>    echo -e "\n====================================================="<br>    echo "AEGIS AIDE AUDIT: $(date --iso-8601=seconds)"<br>    echo "====================================================="<br>    aide --update<br>} >> "$AUDIT_LOG" 2>&1<br>AIDE_EXIT_CODE=$?<br>set -e<br>if [[ -f "$AIDE_DB_NEW" && -s "$AIDE_DB_NEW" ]]; then<br>    sync -f "$AIDE_DB_NEW"<br>    mv -f "$AIDE_DB_NEW" "$AIDE_DB"<br>    sync -f "$AIDE_DB"<br>    if [[ "$SUPPORTS_IMMUTABLE" == true ]]; then<br>        chattr +i "$AIDE_DB" 2>/dev/null || true<br>    fi<br>    if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] Post-süreç bayrağı silinemedi."<br>    fi<br>    systemd-cat -t aegis-aide -p info <<< "[+] AIDE mühürlendi (Exit: $AIDE_EXIT_CODE)."<br>else<br>    systemd-cat -t aegis-aide -p emerg <<< "[FATAL] Yeni veritabanı bulunamadı veya 0-byte!"<br>    exit 1<br>fi<br>exit 0</pre>Sealing permissions:<br><code>sudo chmod 700 /usr/local/bin/aegis-aide-update.sh</code><br><code>sudo chown root:root /usr/local/bin/aegis-aide-update.sh</code> | **Ana Betik İçeriği:**<br>Aşağıdaki üretim planını dosyaya yapıştırın:<br><pre>#!/usr/bin/env bash<br># V18_AEGIS_SURFACE: Asynchronous AIDE Updater & Auditor (V18.1)<br>set -euo pipefail<br>umask 0077<br>FLAG_FILE="/run/aegis_aide_pending.flag"<br>PROCESS_FLAG="/run/aegis_aide_processing.flag"<br>AUDIT_LOG="/var/log/aegis_aide_audit.log"<br>LOCK_FILE="/run/aegis_aide_lock"<br>AIDE_DB="/var/lib/aide/aide.db.gz"<br>AIDE_DB_NEW="/var/lib/aide/aide.db.new.gz"<br>AIDE_DIR="/var/lib/aide"<br>if [[ $EUID -ne 0 ]]; then<br>    systemd-cat -t aegis-aide -p err <<< "[FATAL] Root yetkisi gerekir."<br>    exit 1<br>fi<br>if ! mv "$FLAG_FILE" "$PROCESS_FLAG" 2>/dev/null; then<br>    exit 0<br>fi<br>exec 9>> "$LOCK_FILE"<br>if ! flock -n 9; then<br>    systemd-cat -t aegis-aide -p info <<< "[BİLGİ] Tarama zaten aktif. Durum korunuyor."<br>    if ! touch "$FLAG_FILE" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] Rollback bayrağı dikilemedi."<br>    fi<br>    if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] İşlem bayrağı temizlenemedi."<br>    fi<br>    exit 0<br>fi<br>cleanup() {<br>    local exit_code=$?<br>    if [[ $exit_code -ne 0 ]]; then<br>        systemd-cat -t aegis-aide -p emerg <<< "[KRİTİK] AIDE işlemi çöktü! Kod: $exit_code"<br>        if ! touch "$FLAG_FILE" 2>/dev/null; then<br>            systemd-cat -t aegis-aide -p warning <<< "[UYARI] Durum bayrağı geri yüklenemedi."<br>        fi<br>        if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>            systemd-cat -t aegis-aide -p warning <<< "[UYARI] Süreç bayrağı kaldırılamadı."<br>        fi<br>    fi<br>}<br>trap cleanup EXIT INT TERM<br>SUPPORTS_IMMUTABLE=false<br>if TEST_FILE=$(mktemp "${AIDE_DIR}/.aide_test.XXXXXX" 2>/dev/null); then<br>    if chattr +i "$TEST_FILE" 2>/dev/null; then<br>        SUPPORTS_IMMUTABLE=true<br>        chattr -i "$TEST_FILE" 2>/dev/null<br>    fi<br>    rm -f "$TEST_FILE"<br>else<br>    systemd-cat -t aegis-aide -p warning <<< "[UYARI] chattr testi başarısız."<br>fi<br>if [[ "$SUPPORTS_IMMUTABLE" == true && -f "$AIDE_DB" ]]; then<br>    chattr -i "$AIDE_DB" 2>/dev/null || true<br>fi<br>systemd-cat -t aegis-aide -p info <<< "[+] AIDE Güncelleme ve Denetim Operasyonu Başlatıldı..."<br>set +e<br>{<br>    echo -e "\n====================================================="<br>    echo "AEGIS AIDE AUDIT: $(date --iso-8601=seconds)"<br>    echo "====================================================="<br>    aide --update<br>} >> "$AUDIT_LOG" 2>&1<br>AIDE_EXIT_CODE=$?<br>set -e<br>if [[ -f "$AIDE_DB_NEW" && -s "$AIDE_DB_NEW" ]]; then<br>    sync -f "$AIDE_DB_NEW"<br>    mv -f "$AIDE_DB_NEW" "$AIDE_DB"<br>    sync -f "$AIDE_DB"<br>    if [[ "$SUPPORTS_IMMUTABLE" == true ]]; then<br>        chattr +i "$AIDE_DB" 2>/dev/null || true<br>    fi<br>    if ! rm -f "$PROCESS_FLAG" 2>/dev/null; then<br>        systemd-cat -t aegis-aide -p warning <<< "[UYARI] Post-süreç bayrağı silinemedi."<br>    fi<br>    systemd-cat -t aegis-aide -p info <<< "[+] AIDE mühürlendi (Exit: $AIDE_EXIT_CODE)."<br>else<br>    systemd-cat -t aegis-aide -p emerg <<< "[FATAL] Yeni veritabanı bulunamadı veya 0-byte!"<br>    exit 1<br>fi<br>exit 0</pre>İzinleri mühürleyin:<br><code>sudo chmod 700 /usr/local/bin/aegis-aide-update.sh</code><br><code>sudo chown root:root /usr/local/bin/aegis-aide-update.sh</code> |
| **Step 5: Structural Systemd Automation Units**<br>Construct the decoupled asynchronous engine parameters. By stripping out `PrivateTmp=yes`, it drops sandbox isolation layers that cause local directory lookup mismatches. Explicitly allow `ReadWritePaths=` for AIDE state tracking directly inside system directories.<br><br><code>sudo nano /etc/systemd/system/aegis-aide.service</code> | **Adım 5: Yapısal Systemd Otomasyon Birimleri**<br>Ayrıştırılmış asenkron motor parametrelerini inşa edin. `PrivateTmp=yes` parametresini kaldırarak yerel dizin haritalama uyuşmazlıklarına yol açan boğucu izolasyon katmanlarını temizler. Sistem dizinlerindeki AIDE takibi için `ReadWritePaths=` istisnalarını açıkça yetkilendirir.<br><br><code>sudo nano /etc/systemd/system/aegis-aide.service</code> |
| **Service Execution Block Content:**<br><pre>[Unit]<br>Description=AEGIS AIDE Zero-Trust Decoupled Updater & Auditor<br>ConditionPathExists=/run/aegis_aide_pending.flag<br><br>[Service]<br>Type=oneshot<br>ExecStart=/usr/local/bin/aegis-aide-update.sh<br>IOSchedulingClass=idle<br>IOSchedulingPriority=7<br>Nice=19<br>UMask=0077<br>ReadWritePaths=/var/lib/aide /var/log<br>NoNewPrivileges=yes<br>ProtectKernelModules=yes<br>ProtectKernelTunables=yes<br>ProtectControlGroups=yes<br>RestrictSUIDSGID=yes<br>LockPersonality=yes<br>ProtectClock=yes<br>ProtectHostname=yes<br>PrivateDevices=yes</pre> | **Servis Yürütme Blok İçeriği:**<br><pre>[Unit]<br>Description=AEGIS AIDE Zero-Trust Decoupled Updater & Auditor<br>ConditionPathExists=/run/aegis_aide_pending.flag<br><br>[Service]<br>Type=oneshot<br>ExecStart=/usr/local/bin/aegis-aide-update.sh<br>IOSchedulingClass=idle<br>IOSchedulingPriority=7<br>Nice=19<br>UMask=0077<br>ReadWritePaths=/var/lib/aide /var/log<br>NoNewPrivileges=yes<br>ProtectKernelModules=yes<br>ProtectKernelTunables=yes<br>ProtectControlGroups=yes<br>RestrictSUIDSGID=yes<br>LockPersonality=yes<br>ProtectClock=yes<br>ProtectHostname=yes<br>PrivateDevices=yes</pre> |
| **Deploy the Native Clock Trigger (Timer)**<br>Program the cron-less execution engine profile to handle routine background audits smoothly every Sunday at midnight.<br><br><code>sudo nano /etc/systemd/system/aegis-aide.timer</code><br><br>Unit profile code:<br><pre>[Unit]<br>Description=AEGIS AIDE Weekly SSD-Friendly Timer<br>[Timer]<br>OnCalendar=Sun *-*-* 00:00:00<br>Persistent=true<br>[Install]<br>WantedBy=timers.target</pre> | **Zamanlayıcı Tetikleyici Birimini Dağıtın (Timer)**<br>Her Pazar gece yarısı rutin arka plan denetimlerini sistemi yormadan ve fan gürültüsü üretmeden yürütmek için zamanlayıcı profilini programlayın.<br><br><code>sudo nano /etc/systemd/system/aegis-aide.timer</code><br><br>Zamanlayıcı kod bloğu:<br><pre>[Unit]<br>Description=AEGIS AIDE Weekly SSD-Friendly Timer<br>[Timer]<br>OnCalendar=Sun *-*-* 00:00:00<br>Persistent=true<br>[Install]<br>WantedBy=timers.target</pre> |
| **Step 6: Initialize Persistent Infrastructure & Logs**<br>Establish the append-mode telemetry map, inject proper standard permissions, and run a full SELinux security context update.<br><br><code>sudo touch /var/log/aegis_aide_audit.log</code><br><code>sudo chmod 0600 /var/log/aegis_aide_audit.log</code><br><code>sudo restorecon -v /var/log/aegis_aide_audit.log</code> | **Adım 6: Kalıcı Altyapı ve Günlüklerin Başlatılması**<br>Üzerine ekleme (append) modunda çalışan telemetri haritasını oluşturun, standart sıkı izinleri enjekte edin ve tam bir SELinux güvenlik bağlamı güncellemesi yürütün.<br><br><code>sudo touch /var/log/aegis_aide_audit.log</code><br><code>sudo chmod 0600 /var/log/aegis_aide_audit.log</code><br><code>sudo restorecon -v /var/log/aegis_aide_audit.log</code> |
| **Step 7: Archival Rotation System (Logrotate)**<br>Deploy the log handling profiles to handle chronological roll-overs cleanly without exhausting block storage structures.<br><br><code>sudo nano /etc/logrotate.d/aegis-aide</code><br><br>Rule structure:<br><pre>/var/log/aegis_aide_audit.log {<br>  weekly<br>  rotate 4<br>  compress<br>  delaycompress<br>  missingok<br>  notifempty<br>  create 0600 root root<br>}</pre> | **Adım 7: Arşivsel Döndürme Sistemi (Logrotate)**<br>Blok depolama yapılarını şişirmeden kronolojik log döngülerini temiz bir şekilde yönetmek için logrotate profilini teslim edin.<br><br><code>sudo nano /etc/logrotate.d/aegis-aide</code><br><br>Kural yapısı:<br><pre>/var/log/aegis_aide_audit.log {<br>  weekly<br>  rotate 4<br>  compress<br>  delaycompress<br>  missingok<br>  notifempty<br>  create 0600 root root<br>}</pre> |
| **Step 8: System Activation & Verification Protocol**<br>Commit the definitions to the kernel master runtime tracking stack and register the automated background loop:<br><br><code>sudo systemctl daemon-reload</code><br><code>sudo systemctl enable --now aegis-aide.timer</code><br><br>**Manual Testing:** Fire the engine manually to confirm error-free compilation and clean execution paths:<br><code>sudo touch /run/aegis_aide_pending.flag</code><br><code>sudo systemctl start aegis-aide.service</code><br><code>sudo systemctl status aegis-aide.service</code><br><br>The query metrics must display an exit code profile showing `status=0/SUCCESS` alongside a peaceful `inactive (dead)` oneshot state trace. | **Adım 8: Sistem Aktivasyonu ve Doğrulama Protokolü**<br>Tanımlamaları çekirdek ana çalışma zamanı takip yığınına işleyin ve otomatize edilmiş arka plan döngüsünü kaydedin:<br><br><code>sudo systemctl daemon-reload</code><br><code>sudo systemctl enable --now aegis-aide.timer</code><br><br>**Manuel Test:** Hata derlemelerinden arınmış temiz çalışma yollarını doğrulamak için motoru el ile tetikleyin:<br><code>sudo touch /run/aegis_aide_pending.flag</code><br><code>sudo systemctl start aegis-aide.service</code><br><code>sudo systemctl status aegis-aide.service</code><br><br>Sorgu metrikleri, `status=0/SUCCESS` başarı profilini ve görevini tamamlamış oneshot servislere özgü sakin `inactive (dead)` durum izini ekrana basmalıdır. |
