# Aegis Health Dashboard
## Zero-Entropy Asynchronous Telemetry & Audit Architecture (V3.3.0)

| ENGLISH | TÜRKÇE |
| :--- | :--- |
| **Introduction**<br>This project implements an enterprise-grade, absolute zero-fork, locale-agnostic auditing and telemetry agent built for hardened Linux kernels on Fedora Linux. Optimized for V3.3.0, it stream-processes high-volume log pipelines (systemd journalctl, chronyc, usbguard) inside a single-pass POSIX AWK memory space, eliminating process forks, context switching, and resource starvation on mission-critical deployments. | **Giriş**<br>Bu proje, Fedora Linux üzerindeki katılaştırılmış Linux çekirdekleri için tasarlanmış kurumsal düzeyde, kesinlikle fork'suz ve dil bağımsız bir denetim ve telemetri ajanını hayata geçirir. V3.3.0 için optimize edilen bu yapı, yoğun log akış hatlarını (systemd journalctl, chronyc, usbguard) tek geçişli bir POSIX AWK bellek alanında işleyerek, kritik görev dağıtımlarında süreç çatallanmalarını (fork), bağlam geçişlerini ve kaynak tüketimini tamamen ortadan kaldırır. |
| **Step 1: Verify System Dependencies**<br>Ensure all essential POSIX compliance tools are present before launching the telemetry layer.<br><br><code>command -v systemctl journalctl stat awk grep sysctl tr</code><br><br>The system will automatically validate these binaries on every execution lifecycle to prevent dependency poisoning. | **Adım 1: Sistem Bağımlılıklarını Doğrulayın**<br>Telemetri katmanını başlatmadan önce tüm temel POSIX uyumluluk araçlarının mevcut olduğundan emin olun.<br><br><code>command -v systemctl journalctl stat awk grep sysctl tr</code><br><br>Sistem, bağımlılık zehirlenmesini önlemek için her çalışma döngüsünde bu ikilileri otomatik olarak doğrulayacaktır. |
| **Step 2: Prepare Workspace & Pull Script**<br>Create the dedicated secure directory structure under the root-controlled space and establish the core dashboard environment.<br><br></code><br><code>sudo mkdir -p /var/log/aegis</code><br><code>sudo mkdir -p /run/aegis</code><br><code>cd /usr/local/bin/</code><br><code>sudo curl -O https://raw.githubusercontent.com/Ozhana/Fedora_Hardening/main/HEALTH_DASHBOARD/aegis-status.sh</code> | **Adım 2: Çalışma Alanını Hazırlayın ve Betiği Çekin**<br>Kök kullanıcı (root) kontrollü alan altında özel ve güvenli dizin yapısını oluşturun ve ana panel ortamını kurun.<br><br></code><br><code>sudo mkdir -p /var/log/aegis</code><br><code>sudo mkdir -p /run/aegis</code><br><code>cd /usr/local/bin/</code><br><code>sudo curl -O https://raw.githubusercontent.com/Ozhana/Fedora_Hardening/main/HEALTH_DASHBOARD/aegis-status.sh</code> |
| **Step 3: Secure Execution Sandbox**<br>Enforce strict permission masking (umask 077) and restrict file reading and execution capabilities to the root supervisor only.<br><br><code>sudo chown root:root /usr/local/bin/aegis-status.sh</code><br><code>sudo chmod 0700 /usr/local/bin/aegis-status.sh</code><br><code>sudo chmod 0700 /run/aegis</code> | **Adım 3: Çalışma Alanını Güvenliğe Alın**<br>Sıkı izin maskelemesi (umask 077) uygulayın, dosya okuma ve yürütme yetkilerini yalnızca kök (root) yöneticisiyle kısıtlayın.<br><br><code>sudo chown root:root /usr/local/bin/aegis-status.sh</code><br><code>sudo chmod 0700 /usr/local/bin/aegis-status.sh</code><br><code>sudo chmod 0700 /run/aegis</code> |
| **Step 4: Establish Global Execution Command**<br>Map the security agent to an encapsulated system shortcut for zero-friction management across all terminal layers.<br><br><code>echo "alias aegis='sudo /usr/local/bin/aegis-status.sh'" \| sudo tee -a /root/.bashrc</code><br><code>echo "alias aegis='sudo /usr/local/bin/aegis-status.sh'" >> ~/.bashrc</code><br><code>source ~/.bashrc</code> | **Adım 4: Küresel Çalıştırma Komutunu Atayın**<br>Tüm terminal katmanlarında sıfır sürtünmeyle yönetim sağlamak için güvenlik ajanını kapsüllenmiş bir sistem kısayoluna bağlayın.<br><br><code>echo "alias aegis='sudo /usr/local/bin/aegis-status.sh'" \| sudo tee -a /root/.bashrc</code><br><code>echo "alias aegis='sudo /usr/local/bin/aegis-status.sh'" >> ~/.bashrc</code><br><code>source ~/.bashrc</code> |
| **Step 5: Runtime Telemetry Execution**<br>Trigger a single-pass validation sweep across all internal security frameworks:<br><br><code>aegis</code><br><br>To stream continuous real-time telemetry drops under a zero-overhead loop without invoking process forks:<br><br><code>watch -n 10 -c aegis</code> | **Adım 5: Çalışma Zamanı Telemetri Yürütme**<br>Tüm dahili güvenlik mekanizmaları üzerinde tek geçişli bir doğrulama taraması tetikleyin:<br><br><code>aegis</code><br><br>Süreç çatallanması (fork) başlatmadan, sıfır ek yük içeren bir döngüde kesintisiz ve gerçek zamanlı telemetri akışı sağlamak için:<br><br><code>watch -n 10 -c aegis</code> |
| **Step 6: Historical Audit Inspection**<br>Verify rolling cryptographic operation logs, memory profiles, and structural containment events generated by the core dashboard daemon.<br><br><code>sudo cat /var/log/aegis/aegis-status-$(date +%Y%m%d).log</code> | **Adım 6: Geçmiş Denetim Loglarını İnceleyin**<br>Ana panel arka plan servisi tarafından üretilen kriptografik işlem loglarını, bellek profillerini ve yapısal engelleme olaylarını inceleyin.<br><br><code>sudo cat /var/log/aegis/aegis-status-$(date +%Y%m%d).log</code> |
| **⚠️ CRITICAL: PIPELINE PERFORMANCE METRICS**<br>The Aegis Health Dashboard operates using advanced in-memory multiplexing via safe dircb arrays (lines[idx]) inside POSIX AWK. It completely eliminates the standard Linux pipeline multi-fork vulnerability (printf \| sed \| while read).<br><br>This ensures your Surface Pro 9 hardware maintains zero thermal throttling and maximum battery runtime during intense network profiling and automated hardening loops. | **⚠️ KRİTİK: VERİ HATTI PERFORMANS METRİKLERİ**<br>Aegis Sağlık Paneli, POSIX AWK içindeki güvenli dairesel arabellek dizileri (lines[idx]) aracılığıyla gelişmiş bellek içi çoğullama yöntemiyle çalışır. Standart Linux veri hattındaki çoklu çatallanma zafiyetini (printf \| sed \| while read) tamamen ortadan kaldırır.<br><br>Bu sayede yoğun ağ profilleme ve otomatik sıkılaştırma döngüleri sırasında Surface Pro 9 donanımınızın sıfır termal darboğaz (throttling) ve maksimum pil ömrü ile çalışması garanti edilir. |
